version: '3.9'

services:
  db:
    image: 'postgres:latest'
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: 'root'
      POSTGRES_USER: 'root'
      POSTGRES_PASSWORD: 'root'
    restart: always
    volumes:
      - ./db/postgres:/var/lib/postgresql/data'