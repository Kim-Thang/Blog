version: '3.9'

services:
  db:
    image: 'mysql:lastest'
    ports:
      - '3306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: 'root'
      MYSQL_DATABASE: 'root'      
    restart: always
    volumes:
      - ./db/mysql:/var/lib/mysql'